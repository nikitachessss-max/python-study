# Шпаргалка — сессия 3 (списки) — закрыта 28.03.2026

Кратко, без воды. Детали — в `отчеты/` и в `session3/lesson*.py`.

| Что | Суть |
|-----|------|
| **list** | Список: `[a, b, c]` — несколько значений в одной переменной, порядок важен |
| **Индекс** | Номер элемента **с нуля**: первый — `[0]`, второй — `[1]`, третий — `[2]` |
| **Доступ** | `имя[индекс]` — взять один элемент |
| **Замена** | `имя[индекс] = новое` — элемент на позиции меняется |
| **len** | `len(список)` — сколько элементов; последний индекс: `len - 1` |
| **Срез** | `список[a:b]` — кусок списка; **b не входит**. Примеры: `[:3]`, `[2:]` |
| **for по списку** | `for x in список:` — по одному элементу, без индекса |
| **range vs список** | `range` → счётчик 0, 1, 2…; список → твои значения по очереди |
| **append** | `список.append(x)` — дописать x в конец; список сам меняется |
| **del** | `del список[i]` — убрать элемент с позиции i; правее сдвигается, длина −1 |
| **pop** | `список.pop()` — снять последний (можно `x = ...`); `pop(i)` — с позиции i |
| **while True** | Бесконечный цикл; выход через **break** |
| **break** | «Выйди из цикла прямо сейчас»; код после цикла продолжает работать |
| **range(1, n+1)** | Числа от 1 до n включительно (правая граница не входит) |

**Разминка (while):** `lesson3_while_da.py`, `lesson3_while_sum.py`

**Списки (порядок):** `lesson3_list_intro` → `lesson3_list_assign` → `lesson3_list_len` → `lesson3_list_slice` → `lesson3_list_for` → `lesson3_for_range_vs_list` → `lesson3_wrap_today` → `lesson3_list_append` / `lesson3_append_practice` → `lesson3_list_del_pop` → `lesson3_list_numbered_print`

**while True / break:** `lesson3_while_true_break.py` (два живых варианта) → `lesson3_receipt_unknown_count.py` (чек без заданного n)

**Челленджи (корень):** `challenge_sessions_1_3.py` (эталон), `challenge_sessions_1_2_cafe.py`, `challenge_sessions_1_3_courier.py`

**Итоговый отчёт сессии 3:** `отчеты/2026-03-28_итог_сессия_3.md`
