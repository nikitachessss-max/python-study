# -*- coding: utf-8 -*-
#
# Мини-проект: сессии 1 + 2 + 3 — «Лист доставок курьера»
#
# По каркасу как challenge_sessions_1_3.py (чек с позициями), но контекст другой:
# курьер вносит заказы в лист (список строк), считает общую сумму доставки, печатает
# лист и только после слова «ок» завершает смену.
#
# Запуск:  python challenge_sessions_1_3_courier.py
#
# =============================================================================
# Часть A — сессия 1
# =============================================================================
#
# 1) Имя курьера (строка).
# 2) Приветствие с именем и фразой про доставки.
#
# =============================================================================
# Часть B — сессии 2 и 3
# =============================================================================
#
# 3) Целое n — сколько заказов внести в лист.
# 4) stops = [] (пустой список строк).
# 5) total = 0.0 (сумма плат за доставку по всем заказам).
# 6) for i in range(1, n + 1):
#    адрес (строка), плата за доставку (float), прибавить к total,
#    собрать строку с номером i, адресом и платой, append в stops.
#
# 7) Вывести общую сумму доставок за рейс.
#
# =============================================================================
# Часть C — сессия 3
# =============================================================================
#
# 8) len(stops) — сколько остановок в листе.
# 9) Если остановок >= 3: подпись и срез первых трёх stops[:3].
# 10) for по stops — напечатать каждую строку (лист построчно).
#
# =============================================================================
# Часть D — сессия 2
# =============================================================================
#
# 11–14) Сообщить секретное слово для выхода: ок (маленькими).
#     Пока ввод не равен "ок" — просить снова. Потом прощание по имени.
#
# =============================================================================
# Как работать
# =============================================================================
#
# Сначала посмотри и разбери готовый код в challenge_sessions_1_3.py (чек с позициями) —
# здесь тот же порядок шагов: список строк, total, цикл, len, срез, for, while,
# только другие слова в сценарии (курьер / адрес / доставка / слово «ок»).
# Готового кода в этом файле нет — пиши сам ниже.
#
# =============================================================================
# --- КОД ---

name = input("Привет. как тебя зовут?")
print(f"рейс начат, {name}. Заполняем лист доставок!")

n = int(input("сколько доставок внесли в лист?"))
steps = []
total = 0.0

for i in range(1, n + 1):
    adress = input("введите адрес: ")
    cost = float(input("плата за доставку?"))
    total = total + cost
    step = f"{i}. {adress} - {cost} руб."
    steps.append(step)

print(f"общая сумма доставок {total} руб. ")
print(f"количество доставок: {len(steps)} ")

if  len(steps) >= 3:
    print("первые три строчки чека(срез)")
    print(steps[:3])

print("---лист---")
for i in steps:
    print(i)
print('Для завершения введите секретное слово: ок (маленькими буквами)')
word = input("Введите слово: ").strip().lower()
while word != "ок":
    print("Неверно. Введите слово: ок")
    word = input("Введите слово: ").strip().lower()

print(f"Смена окончена, {name}. Удачи на дороге!")