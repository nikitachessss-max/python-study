# -*- coding: utf-8 -*-
#
# Сессия 3 — списки (list): введение и индексы
#
# --- ОБЪЁМ ТЕМЫ (чтобы не было сюрпризов) ---
#
# Списки в Python — это упорядоченная последовательность значений в одной переменной.
# Сейчас пройдём: как создать список, как взять элемент по номеру (индексу), что индекс
# считается с нуля.
#
# Позже в этой сессии (отдельными файлами): срезы [a:b], длина списка len(), ещё задачи.
# Ещё позже: методы вроде .append() — подключим, когда база на индексах сядет в голову.
#
# --- ПРОСТЫМИ СЛОВАМИ ---
#
# Список записывают в квадратных скобках, элементы через запятую:
#   [значение1, значение2, значение3]
#
# Элементы могут быть строками, числами или смесью типов — на старте удобно тренироваться
# на строках или целых числах.
#
# Индекс — это номер позиции внутри списка. В Python первый элемент имеет индекс 0,
# второй — 1, третий — 2. Обращение: имя_списка[индекс]
#
# Мини-пример для ориентира (не твоя домашняя часть, просто образец синтаксиса):
#   fruits = ["яблоко", "груша", "слива"]
#   print(fruits)      # весь список
#   print(fruits[0])   # первый элемент — яблоко
#   print(fruits[2])   # третий элемент — слива
#
# --- ЗАДАЧА В ЭТОМ ФАЙЛЕ ---
#
# 1) Создай переменную со списком из ровно трёх строк — например три имени или три города
#    (любой текст в кавычках, на твой выбор).
#
# 2) Выведи на экран весь список целиком (один print).
#
# 3) Выведи на экран только второй по счёту элемент списка для человека — то есть тот,
#    у которого в Python индекс 1 (первый — 0, второй — 1).
#
# Проверка: в выводе сначала видно три элемента в квадратных скобках, затем одна строка —
# второй элемент.
#
# --- ЗАПУСК ---
#
#   python session3/lesson3_list_intro.py
#
# --- КОД ---
#
# Пиши ниже. Если что-то не сойдётся — напиши в чат.

city = ["Новосибирск", "Москва", "Питер"]
print (city)
print (city[1])