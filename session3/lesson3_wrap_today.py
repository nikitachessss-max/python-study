# -*- coding: utf-8 -*-
#
# =============================================================================
# СЕССИЯ 3 (день) — ТЕЗИСЫ: что прошли сегодня
# =============================================================================
#
# • Разминка while: «пока не да», сумма чисел до 0 — session3/lesson3_while_da.py,
#   lesson3_while_sum.py
#
# • Список list: квадратные скобки, индекс с нуля, чтение и замена элемента
#   — lesson3_list_intro.py, lesson3_list_assign.py
#
# • len(список) и последний элемент по индексу len - 1 — lesson3_list_len.py
#
# • Срезы [a:b], правый конец не входит, [:3] и т.п. — lesson3_list_slice.py
#
# • for по списку: переменная = очередной элемент — lesson3_list_for.py
#
# • Закрепление: for + range (счётчик / число кругов) VS for + список (твои значения)
#   — lesson3_for_range_vs_list.py
#
# • Сегодня НЕ шли: .append(), удаление из списка, индекс внутри цикла — после отдыха.
#
# =============================================================================
# ПОСЛЕДНЕЕ ЗАДАНИЕ НА СЕГОДНЯ (сделай под этим блоком, потом сохрани и запусти)
# =============================================================================
#
# Собери в ОДНОЙ программе (один файл, один запуск) всё ниже — по порядку:
#
# 1) Создай список из четырёх строк (любой смысл).
#
# 2) Выведи весь список одним print.
#
# 3) Выведи длину списка (len).
#
# 4) Выведи последний элемент через индекс (длина минус 1), без магического «3» в
#    индексе — через переменную с len.
#
# 5) Выведи срез: со второго по четвёртый элемент для человека — то есть индексы
#    1 и 4 в записи [1:4] (помни: 4 не входит, получится три элемента).
#
# 6) Цикл for по этому же списку: на каждой строке выведи f"— {переменная}" (черточка
#    и пробел и элемент).
#
# 7) Отдельным циклом: for по range(3) — внутри только print("круг", i) или как тебе
#    удобно с f-строкой, главное видно числа 0, 1, 2. Так закрепляешь: это range,
#    не элементы списка.
#
# Запуск:
#   python session3/lesson3_wrap_today.py
#
# --- КОД ---
city = ["москва", "питер", "казань","новосиб"]
print(city)
n = len(city)
print(n)
last = city[n - 1]
print(last)
print(city[1:4])
for s in city:
    print(f"— {s}")

for i in range(3):
    print("круг", i)
